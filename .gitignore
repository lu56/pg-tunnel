# SSH private keys - NEVER commit
*_ed25519
*_ed25519.pub
*.pem
*.key
id_*

# Environment files (contain public key)
.env

# SSH server host keys (generated at runtime, persisted via volume)
host_keys/

# Synology metadata
@eaDir/
.DS_Store

# IDE
.vscode/
.idea/
